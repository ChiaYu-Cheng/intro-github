name: Run Script

on: 
  workflow_dispatch:

jobs:
  example-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Run build script
        run: ./.github/scripts/hello-world.sh
